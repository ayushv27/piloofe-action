# Development Environment
NODE_ENV=development
APP_PORT=5000
BUILD_TARGET=dev

# Database Configuration
POSTGRES_DB=piloo_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=devpassword
POSTGRES_PORT=5432

# Django Backend API Configuration (Development)
DJANGO_API_ROOT=http://localhost:8000/api
DJANGO_PORT=8000
DJANGO_DEBUG=true

# Redis Configuration
REDIS_PASSWORD=devredispass
REDIS_PORT=6379

# Development Volume Mount
DEV_VOLUME_MOUNT=./

# Session Security (Development - use a simple key)
SESSION_SECRET=dev-session-secret-key-not-for-production